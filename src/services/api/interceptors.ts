/**
 * Additional API interceptors (can be extended)
 */

import { apiClient } from './client';

/**
 * Example: Add custom interceptor for retry logic
 * This can be extended based on your needs
 */

// You can add more interceptors here or extend the client
// For example, retry logic, request queuing, etc.

export { apiClient };

